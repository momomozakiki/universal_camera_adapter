# 2026-W30  (2026-07-20 – 2026-07-26)

## 2026-07-20

### [code] scripts/discover_onvif_cameras.py, scripts/test_rtsp_connection.py — standalone RTSP/ONVIF connectivity diagnostics
- **What:** Two stdlib-only Python diagnostic scripts, outside the `CameraAdapter` contract:
  `discover_onvif_cameras.py` (WS-Discovery UDP multicast probe to `239.255.255.250:3702`,
  parses `ProbeMatch` XAddrs) and `test_rtsp_connection.py` (raw RTSP `OPTIONS`/`DESCRIBE`
  over TCP, Digest RFC 2617 auth with MD5 + `qop=auth` support, Basic fallback, clear
  reachable/auth/stream-found verdict).
- **Why:** User has a real EZVIZ CS-H6c on the LAN and wants to verify network connectivity
  before/independent of the ONVIF backend implementation (Epic 2, still `UnimplementedError`
  scaffolding with no `http`/`xml`/`media_kit` deps declared). Building out the real ONVIF
  backend just to answer "can I reach this camera" would be premature; a small diagnostic
  script answers it now and gives real target params (host/creds/path) for when Epic 2 work
  starts. No `ffmpeg`/`opencv-python` on this machine, so kept dependency-free.
- **Verified:** Ran discovery live against the user's LAN — found and correctly parsed replies
  from two *other* WS-Discovery-capable devices (a Windows WSD endpoint, a network printer)
  after fixing an initial WS-Addressing namespace bug (used 2004/08 instead of the 2005/08
  namespace this network's devices expect) in the probe template. The EZVIZ camera itself did
  not answer WS-Discovery — consistent with the plan's documented risk that ONVIF/discovery
  may be off or unsupported on this model/firmware. Manual IP lookup (EZVIZ app / router DHCP
  list) is the fallback in progress; `test_rtsp_connection.py` is not yet run against the real
  camera pending that IP.
- **Refs:** plan `create-a-testing-connect-serialized-kazoo.md` · branch `feat/camera-v1`

## 2026-07-21

### [code] scripts/ezviz_bridge.py, example/lib/tabs/ezviz_tab.dart — working EZVIZ live-view test path via Open Platform + native SDK player
- **What:** Full pivot away from the local-SDK approach (`pyezvizapi`, ports 9010/9020),
  which was conclusively a dead end — the library's decoder doesn't understand this
  camera's "IDMX" local-stream packet container at all, independent of encryption state.
  `scripts/ezviz_bridge.py` rewritten (stdlib-only, `pyezvizapi` dependency dropped
  entirely) around EZVIZ's official Open Platform cloud API: `GET /devices` →
  `device/list`, `GET /config` → `{appKey, accessToken}` from a cached/auto-refreshed
  `token/get`. `example/lib/tabs/ezviz_tab.dart` rewritten to consume this: device list
  → verification-code field + explicit Connect/Reconnect button (keyed
  `ValueKey(serial::code::attempt)` forcing a fresh player instance per tap, avoiding any
  dependency on which button the user taps) → live video via a new small
  `_EzvizNativePlayer` widget.
- **Why:** User wants a reusable, modular Flutter test surface for viewing this camera's
  live feed, for any EZVIZ account/camera, not just this one. HLS/RTMP
  (`live/address/get` protocol=2/3) were tried first and empirically ruled out for this
  account/region (fixed error-placeholder segment; RTMP relay refuses connection) — only
  `ezopen://` (protocol=1) works, and needs EZVIZ's own player logic to resolve, not a
  standard video URL. Evaluated three player approaches in order: embedding EZVIZ's
  EZUIKit JS player in a `webview_flutter` WebView (abandoned — `webview_flutter_windows`
  needs a C++23 compiler this machine's MSVC toolchain doesn't have); the `ezviz_flutter`
  pub package's `EzvizSimplePlayer` convenience widget (added, but had a confirmed logic
  bug — see below); finally `ezviz_flutter`'s lower-level `EzvizPlayer`/
  `EzvizPlayerController` primitives, wrapped in this repo's own thin `_EzvizNativePlayer`.
- **Bugs found and worked around (both in `ezviz_flutter` 1.2.7, not this repo's code):**
  1. `EzvizSimplePlayer._handlePlayerStatus()` marks its internal `_isPlayerInitialized`
     flag `true` as soon as the native SDK reports status `1` ("Init") — which fires
     immediately after the platform view is created, unconditionally — before
     `_initializePlayer()` (the only place that calls `setPlayVerifyCode()`) has ever run.
     `_startLiveStream()` only calls `_initializePlayer()` `if (!_isPlayerInitialized)`,
     so the verification code is silently never applied, for both `autoPlay` and manual
     retry. Confirmed via `adb logcat`: the plugin's own `🔐 Setting verification code`
     debug line never appeared across ~9 real playback attempts on real hardware. Worked
     around by bypassing `EzvizSimplePlayer` entirely.
  2. The native Kotlin side (`EzvizView.kt: onMethodCall`) never calls
     `result.success()`/`result.error()` for `initPlayerByDevice`, `setPlayVerifyCode`, or
     `startRealPlay` (unlike e.g. `openSound`/`capturePicture`, which do) — awaiting these
     calls from Dart hangs forever. `_EzvizNativePlayer` fires them without awaiting;
     actual playback progress/errors are reported via the separate event channel (already
     wired via `setPlayerEventHandler`), not these calls' return values.
- **Verified end-to-end on real hardware:** phone `CPH2113` (Android 12, USB-connected,
  `adb reverse tcp:8765 tcp:8765` to reach the bridge on this laptop). Bridge's
  `/devices`/`/config` confirmed via curl. App rebuilt/reinstalled repeatedly during
  debugging; final build reaches native SDK `startRealPlay` with `setPlayVerifyCode`
  correctly applied (confirmed via the app's own `Applying verification code` debug line
  and sustained ~16fps virtual-display compositing in logcat while connected) and the
  user confirmed live camera connection succeeded. `flutter analyze --fatal-infos` clean
  throughout. Also added: `EzvizTab` bridge-host/verification-code fields persisted via
  `shared_preferences` across app restarts; `example/windows/` scaffolding (via
  `flutter create --platforms=windows .`) so the example app builds on Windows too
  (this EZVIZ tab is Android/iOS-only — `ezviz_flutter` has no Windows implementation,
  same constraint that ruled out `webview_flutter_windows` above).
- **Refs:** plan `now-i-want-to-wild-simon.md` · branch `feat/camera-v1`

## 2026-07-22

### [code] example/lib/tabs/ezviz_tab.dart, example/third_party/ezviz_flutter — native per-user EZVIZ login, replacing the bridge
- **What:** Replaced the single-shared-account, laptop-bridge-dependent login flow with EZVIZ's
  own native per-user login: `EzvizAuthManager.openLoginPage()` launches EZVIZ's hosted login
  page (this app never sees a password), `getAccessToken()`/`logout()` manage the resulting
  session, `EzvizDeviceManager.getDeviceList()` replaces the bridge's `/devices` call. New
  `_Step.signIn` step added to `ezviz_tab.dart`; `scripts/ezviz_bridge.py` left in place
  (unreferenced) per explicit instruction, to be deleted once this flow is fully confirmed.
  `ezviz_flutter` is no longer pulled from pub.dev — vendored into
  `example/third_party/ezviz_flutter` (pruned to `lib`/`android`/`ios` only) so four real native
  bugs could be patched directly; `example/pubspec.yaml` now points at it via `path:`, and
  `example/analysis_options.yaml` excludes `third_party/**` from lint (third-party code, not
  ours to restyle).
- **Why:** User's explicit ask: "each user will login at their own account later so it must
  support login feature for each user... the info must be at the phone app with security
  feature." Confirmed via native source (`EzvizManager.kt`) that only the AppKey (public) is
  needed client-side; the AppSecret is never touched by this flow.
- **Bugs found and fixed (all in `ezviz_flutter` 1.2.7 / its native SDK dependency, not this
  repo's original code — each confirmed via `adb logcat` before patching, not guessed):**
  1. `io.github.ezviz-open:ezviz-sdk:5.27`'s AAR ships an empty `AndroidManifest.xml` (zero
     `<activity>` entries) despite compiling in `EzvizWebViewActivity`/`EZAuthHandleActivity` —
     `openLoginPage()` failed with "Unable to find explicit activity class". Fixed by declaring
     both activities in `example/android/app/src/main/AndroidManifest.xml`.
  2. `initSDK`'s native handler called `EZGlobalSDK.getInstance().setAccessToken(accessToken)`
     unconditionally, even when called with `accessToken: ''` — and this tab's own bootstrap
     called `getAccessToken()` *before* `initSDK()` (which throws natively since the SDK
     singleton doesn't exist yet; the Dart wrapper swallows this to `null`), so every cold start
     fell into the "no token" branch and immediately erased the token `initLib()` had just
     silently restored from disk. Every fresh launch required signing in again. Fixed both
     sides: vendored `initSDK` now only calls `setAccessToken` when non-empty; `_bootstrap` now
     calls `initSDK` before `getAccessToken`.
  3. `getDeviceList`'s native mapping sent a nested `cameraInfoList` shape with a raw
     `EZConstants.EZVideoLevel` enum value (e.g. `VIDEO_LEVEL_4K`) — unserializable by Flutter's
     `StandardMessageCodec`, crashing the whole process.
  4. That nested shape was moot anyway: `EzvizDeviceManager.getDeviceList()` actually decodes
     into `ezviz_definition.dart`'s `EzvizDeviceInfo`, a flat model requiring
     `deviceSerial`/`deviceName`/`isSupportPTZ`/`cameraNum` — a completely different, unrelated
     model from the `DeviceInfo`/`CameraInfo` pair in `models.g.dart` that bug 3's fix targeted.
     The real crash was a missing `cameraNum` key, always `null` against a non-nullable `int`
     field ("type 'Null' is not a subtype of type 'int'"). Fixed by rewriting the native mapping
     to the flat shape Dart actually reads.
- **Verified end-to-end on real hardware** (phone `CPH2113`, USB): sign-in → hosted EZVIZ login
  page → device list populated with the signed-in account's own camera → sign-out returns to
  sign-in. Not yet re-verified this session: playback (`_EzvizNativePlayer`, unchanged, expected
  unaffected) and force-quit/relaunch token persistence (expected fixed by bug 2's fix, not
  explicitly re-tested). `flutter analyze --fatal-infos` clean.
- **Deferred (explicit user instruction, unchanged from 2026-07-21):** `scripts/ezviz_bridge.py`
  stays until this flow is fully confirmed, then gets deleted together with rewriting
  `docs/camera/ezviz-integration-notes.md` to describe this architecture as current.
- **Refs:** plan `now-i-want-to-wild-simon.md` · branch `feat/camera-v1`

## 2026-07-22

### [design] docs/camera/, docs/plan/ROADMAP.md, history/ — discovery pipeline, feature matrix, camera profiles, modular wizard, EZVIZ per-user design

- **What:** Finalized architecture design for multi-backend discovery, expanded camera capabilities
  model (tri-state feature support), per-user camera profile persistence, modular add-camera wizard
  abstraction, and corrected per-user EZVIZ onboarding flow (leveraging the native SDK hosted login
  confirmed working this week on real hardware). Authored five new documentation files: (1)
  `docs/camera/discovery-pipeline.md` — three-stage observable discovery (OS → local hardware →
  external/network probes/cloud list); (2) `docs/camera/feature-matrix.md` — `CameraFeature` enum,
  tri-state `CameraFeatureStatus`, `CameraFeatureBundle` lookup, `CameraFeatureMatrix` wrapper,
  migration path for `CameraCapabilities`; (3) `docs/camera/camera-profiles.md` — `CameraProfile`
  + injectable `CameraProfileStore` (shared_preferences default) + `CameraSecretStore`
  (flutter_secure_storage default), default-camera selection, multi-backend split; (4)
  `docs/camera/ezviz-setup-guide.md` — step-by-step per-user EZVIZ onboarding (native SDK hosted
  login confirmed working on real hardware; verification codes in secure storage; remaining
  operational items flagged for re-verification; note on vendored `ezviz_flutter` and 4 upstream
  bugs); (5) `docs/camera/add-camera-wizard.md` — `CameraSetupWizard` abstract + `CameraSetupWizardRegistry`,
  wizard factory pattern for setup UI, example Cameras tab description.
- **Why:** These are foundational designs that unblock multi-backend discovery, capabilities
  querying, and per-backend setup wizards — all prerequisites for production EZVIZ + ONVIF
  backends coexisting cleanly. Design surfaces the native-login mechanism (confirmed working),
  clarifies what's still pending operational re-verification (playback, persistence, sign-out),
  and explicitly documents the vendored, patched `ezviz_flutter` and its upstream issues as a
  known technical debt requiring upstream porting or long-term vendoring decision.
- **Verified:** Design validated against the session's actual EZVIZ implementation outcomes:
  native SDK hosted login (working, tested on real hardware CPH2113); 4 upstream bugs found and
  patched (AAR manifest, token clobbering, getDeviceList shape issues); remaining items (playback,
  persistence, sign-out) flagged for re-verification, not assumed fixed. Architecture fits the
  real constraints and is cross-checked against existing code (adapter contract, registry pattern,
  profile/secret separation).
- **Updated:** `docs/camera/camera-integration-architecture.md` — added "Planned extensions"
  section summarizing the four new docs + EZVIZ adapter. `docs/plan/ROADMAP.md` — inserted Epic
  2.5 (discovery/feature-matrix/profiles/wizard foundation) and Epic 2.6 (EzvizCameraAdapter,
  depends on 2.5, blocking items for native-login re-verification + frame-capture spike + bridge
  retirement decision).
- **Not modified this round:** `docs/camera/ezviz-integration-notes.md` (left as-is, historical
  record; supersession banner and retirement decision deferred until native flow fully confirmed per
  user instruction).
- **Refs:** plan `now-the-external-camera-fancy-boole-agent-aad67bf61867e22bc.md` · no commit yet
  (design-only session, per scope)

### [design] history/ — native EZVIZ login implementation outcomes (parallel session; not committed)

- **What:** Parallel session (distinct from the design work above) completed a full native EZVIZ
  per-user login rewrite on the test device (CPH2113, Android 12), replacing the old single-shared-account
  bridge flow. Sign-in → hosted EZVIZ login page (rendered entirely by EZVIZ's native SDK) →
  device list populated successfully (end-to-end verified via `adb logcat`). Required vendoring and
  patching `ezviz_flutter` 1.2.7 (no longer pulled from pub.dev) at `example/third_party/ezviz_flutter/`,
  with four bugs confirmed via real hardware and fixed in the vendored Kotlin:
  (1) Native AAR missing login activity declarations in AndroidManifest.xml (fixed by explicit
  declaration in example app's manifest); (2) Token clobbering on cold start via unconditional
  `setAccessToken('')` (fixed both app-side bootstrap order and vendored Kotlin's `initSDK`);
  (3/4) `getDeviceList` crash due to shape mismatch (nested struct with raw enum vs. flat shape,
  fixed by rewriting Kotlin-side mapping to match Dart's `EzvizDeviceInfo` deserializer).
- **Not yet re-verified:** Playback (should work, same appKey/token/serial contract, but hasn't
  been re-clicked through since login rewrite); token persistence post-cold-start (fix #2 should
  resolve it, but explicit re-test pending); sign-out flow (tested once, needs re-confirmation).
- **Pending decision:** Upstream porting of the 4 fixes vs. long-term vendoring of the patched
  plugin; retirement of `scripts/ezviz_bridge.py` and supersession/update of
  `docs/camera/ezviz-integration-notes.md` (deferred until native flow fully confirmed per user
  instruction — not done this session).
- **Status:** Working implementation on real hardware, but operational re-verification required
  before production readiness. Nothing committed/pushed this session; awaiting user sign-off before
  any changes land on `feat/camera-v1`.
- **Refs:** plan `now-i-want-to-wild-simon.md` (prior session) · branch `feat/camera-v1` (staging,
  not pushed)

### [workflow] .claude/skills/state-management/SKILL.md, CLAUDE.md, .claude/skills/adaptive-workflow/SKILL.md — new state-management skill + conditional trigger

- **What:** Added a new `state-management` skill (modeled on `input-hardening`'s structure) with
  five rules: decide in-memory vs. persisted per piece of state (distinguishing ephemeral UI state
  from user-meaningful settings); extend the existing `CameraSession` container instead of adding
  a competing one; treat loaded/persisted values as untrusted input (default gracefully on
  missing/malformed/old-version data); re-validate a restored device ID against the live device
  list before use; verify restoration with an actual restart, not just the save call. Registered
  it in `CLAUDE.md`'s skills list and added a new Phase 2 conditional-trigger row in
  `adaptive-workflow/SKILL.md` ("New/changed UI state... → Apply [[state-management]]").
- **Why:** User's explicit request: as the example app grows past a bare testing toolkit, new
  screens/state must not silently reset selection/settings across rebuilds or restarts (a UX
  regression risk), and this should be enforced as a durable workflow habit — a dedicated skill,
  not just a one-line reminder — following the project's existing skills-first pattern.
- **Verified:** Cross-links (`[[camera-adapter-authoring]]`, `[[dart-solid-principles]]`,
  `[[input-hardening]]`) resolve to existing skill names; no example-app code was touched (scoped
  to workflow/docs only, per user decision).
- **Refs:** plan `since-we-are-designing-vast-newt.md` · branch `feat/camera-v1`

## 2026-07-22 (continued)

### [verify] example/lib/tabs/ezviz_tab.dart — on-hardware re-verification of playback + token persistence; frame-capture spike resolved

- **What:** Closed out the two remaining Epic 2.6 blocking items from the native-login rewrite,
  live on the test phone (CPH2113) over `adb`. (1) Playback: opened "Scale Tech Cam"
  (serial BK0381480) from the device list, tapped Connect, confirmed `Player state: playing` with
  live video rendering (screencap shows a real-time frame, timestamp matching wall-clock). (2)
  Token persistence: `adb shell am force-stop` on the running app (confirmed dead via `pidof`),
  cold-relaunched via `adb shell am start`, navigated to the EZVIZ tab — landed directly on the
  device list, no re-login prompt, confirming the token survives a real process kill (not just
  backgrounding). Also resolved the frame-capture spike flagged in `feature-matrix.md` §"platform-view
  frame-capture question": read the vendored plugin's native source
  (`EzvizPlayerView.kt`/`EzvizView.kt`) and a decompiled copy of the real EZVIZ Android SDK
  (`com.videogo.openapi.EZPlayer`, previously extracted to `/tmp/ezvizsdk_classes`) — `capturePicture`
  is a real, working method-channel call (unlike the three broken ones), but the vendored plugin's
  own implementation is a stub that always returns `null`; the actual SDK has a working
  `EZPlayer.capturePicture(int): Bitmap` the plugin never calls. Not a platform-view dead end —
  a plugin-wiring gap with a known fix.
- **Why:** Epic 2.6's "next action" required these three items resolved before treating the
  native-login rewrite as production-ready; all three were open questions, not assumed answers.
- **Verified:** All three confirmed live against real hardware/source, not inferred — see above.
- **Updated:** `docs/plan/ROADMAP.md` Epic 2.6 checklist (playback/persistence/frame-capture-spike
  now checked off, with detail) and "Next action" line; `docs/camera/feature-matrix.md`'s EZVIZ
  section and platform-view question rewritten from "flagged, not resolved" to resolved with the
  concrete finding.
- **Remaining for Epic 2.6:** decide upstream-patch-vs-vendor for the 4 confirmed `ezviz_flutter`
  bugs; decide `scripts/ezviz_bridge.py` / `docs/camera/ezviz-integration-notes.md` retirement
  timing; patch the vendored plugin's `capturePicture` to use the real SDK call; build the actual
  `EzvizCameraAdapter`/`EzvizSetupWizard` behind the `CameraAdapter` contract (today's working code
  is still the standalone test tab, not yet behind the contract).
- **Refs:** branch `feat/camera-v1` (nothing committed this session; user asks before any commit)

### [decision] docs/plan/ROADMAP.md — vendor-now, upstream-later for the 4 ezviz_flutter bugs

- **What:** Decided the `ezviz_flutter` upstream-patch-vs-vendor question: keep shipping on the
  vendored, patched copy (`example/third_party/ezviz_flutter/`) indefinitely — it already works and
  costs nothing extra — while separately filing the plugin-side fixes (token clobbering,
  `getDeviceList` shape, and the `capturePicture` wiring once done) as a PR against upstream
  `ezviz_flutter`, with no expectation of a merge timeline and no implementation work blocked on it.
- **Why:** Vendoring alone ships now with full control over the next fix (e.g. `capturePicture`)
  but means owning an ~12k-line third-party plugin indefinitely. Waiting on an upstream PR alone
  blocks on an unknown maintainer's timeline for a small/single-maintainer package. Doing both in
  parallel, non-blocking, gets the upside of each with no added risk — filing the PR costs nothing
  and can be dropped later if it lands.
- **Updated:** `docs/plan/ROADMAP.md` Epic 2.6 checklist — marked the vendor/upstream decision
  done, added a follow-up (non-blocking) item to actually file the PR.
- **Refs:** branch `feat/camera-v1` (nothing committed this session; user asks before any commit)

### [code] example/lib/ezviz/ezviz_camera_adapter.dart — EzvizCameraAdapter behind the CameraAdapter contract

- **What:** Implemented `EzvizCameraAdapter extends CameraAdapter`: `listDevices()` maps
  `EzvizDeviceInfo` → `CameraDevice` (`brand`/`isSupportPTZ`/`cameraNum` in `metadata`); `open()`
  reads the natively-cached token via `EzvizAuthManager.getAccessToken()` (throws `StateError` if
  no user is signed in — sign-in itself stays out of scope for this adapter) and takes an optional
  verification code via `device.metadata['verificationCode']` (the contract has no dedicated
  parameter for it); `buildPreview()` returns an `EzvizPlayer` wired to the same un-awaited
  `initPlayerByDevice → setPlayVerifyCode → startRealPlay` sequence proven in `ezviz_tab.dart`
  (those three native calls never resolve their method-channel future — documented in both files);
  `captureFrame()` calls the real `EzvizPlayerController.capturePicture()` and reads the returned
  file path into bytes, but throws a clear `StateError` today since the vendored native stub still
  returns `null` (matches `feature-matrix.md`'s `unvalidated` status — will start working once the
  `capturePicture` patch lands). `setZoom` throws `UnsupportedError` (not implemented); `setPan`/
  `setTilt` inherit the base class's default throw. Registered in `example/lib/main.dart`'s
  `buildRegistry()` as `'ezviz'` (not default).
- **Why:** The core deliverable of Epic 2.6 still outstanding after the native-login hardware
  verification: today's proven EZVIZ code lived entirely in a standalone test tab, outside the
  `CameraAdapter` abstraction (not registered, no `CameraSession` integration). This is the first
  step toward a real, contract-conformant EZVIZ backend other apps can consume via the Golden Rule.
- **Architecture decision (asked the user, not assumed):** the adapter lives in the **example app**,
  not the main `lib/` package. `pub publish` rejects path/git dependencies, and `ezviz_flutter` is
  only usable via the vendored, patched copy today (a `path:` dep in `example/pubspec.yaml`) — so
  putting `EzvizCameraAdapter` in `lib/` would make the whole package unpublishable. Move it into
  `lib/` later once either the upstream PR merges (pub.dev-installable `ezviz_flutter`) or a
  permanent-vendoring decision is made (e.g. a separate companion pub package).
- **State-management check:** the adapter's new fields (`_device`, `_accessToken`,
  `_verificationCode`, `_controller`) are internal adapter runtime state — same category as
  `FlutterCameraAdapter`'s internal `CameraController` field — not a competing state container
  next to `CameraSession`, and not new user-facing "settings" requiring a persistence decision.
  The two things that do need to survive a restart (the access token; the verification-code text
  field) are unchanged and already decided: the token persists natively (already verified via the
  force-quit/relaunch hardware test), and the code persists via `shared_preferences` in
  `ezviz_tab.dart`, untouched by this change.
- **Verified:** `flutter analyze --fatal-infos` on `example/` — no issues found.
- **Not done this round:** `EzvizCameraAdapter` is not yet reachable from the bottom-nav toolkit UI
  (no `EzvizSetupWizard`, no `CameraSession.switchTo()` — both still Epic 2.5/2.6 follow-ups);
  `ezviz_tab.dart` stays in place until the wizard can fully replace it.
- **Refs:** `docs/plan/ROADMAP.md` Epic 2.6 checklist + "Next action" line updated · branch
  `feat/camera-v1` (nothing committed this session; user asks before any commit)
