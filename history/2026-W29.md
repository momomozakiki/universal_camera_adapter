# 2026-W29  (2026-07-13 – 2026-07-19)

## 2026-07-18

### [workflow] .claude/ — adopt the Adaptive Self-Correcting Workflow (vendored)
- **What:** Vendored workflow-core into this new repo: `.claude/hooks/workflow_hook.py`
  (stdlib-only SessionStart/PostToolUse/Stop dispatcher), `.claude/workflow_config.json`
  (source dirs `lib`/`test`, docs dirs, roadmap `docs/plan/ROADMAP.md`, env_check for
  dart/flutter/python, ledger enabled), `.claude/schemas/` (config schema + hook contract), and the
  hook unit tests at `tests/hook/test_hook.py` (import path retargeted to `.claude/hooks/`).
- **Why:** Establish the same versioned engineering discipline (fixed invariants, per-task
  checklists, ambient hook reminders, change ledger, provenance) used in `odb_library` from day one.
- **Refs:** plan `below-is-the-camera-humble-sun.md` · branch `feat/adopt-workflow`

### [config] .claude/settings.json — hooks + permission allowlist
- **What:** Created the committed `settings.json` with the three workflow hooks **and** a
  `permissions.allow` list covering read-only planning commands plus this project's tools
  (`flutter analyze/test/pub get`, `dart analyze/test/format`, `python -m unittest`, git read-only,
  `find`). Left the pre-existing `settings.local.json` (personal allowlist) untouched.
- **Why:** Fix the reported plan-mode pain — Claude Code was prompting to approve every read-only
  Bash command because no `permissions` block existed. Deliberately avoided `bypassPermissions`.
- **Refs:** plan `below-is-the-camera-humble-sun.md`

### [workflow] .claude/skills/ — import four adapted skills
- **What:** Added `adaptive-workflow` (near-verbatim; GUIDE/CONTRIBUTING refs retargeted to the
  upstream repo since we vendored), `camera-adapter-authoring` (de-scoped from the odb monorepo to
  this single-package layout; ONVIF/RTSP brought in-scope with an input-hardening lens),
  `dart-solid-principles` (examples reworked around `CameraAdapter`/`CameraAdapterRegistry`), and
  `input-hardening` (retargeted from ODB config/IPC to ONVIF SOAP/XML/RTSP untrusted input).
- **Why:** Carry over the proven authoring/quality/security guidance, adapted to this package.
- **Refs:** plan `below-is-the-camera-humble-sun.md`

### [doc] docs/, history/, templates/ — documentation conventions
- **What:** Seeded the themed `docs/` tree with an index (`docs/README.md`), the provenance policy
  (`docs/DOC-PROVENANCE.md`), the canonical `docs/plan/ROADMAP.md` (with an auto-surfaced
  `**Next action:**`), the weekly ledger format (`history/FORMAT.md`) + this file, the doc
  templates (`templates/docs/`), and the two workflow reference docs (Progressive Disclosure guide,
  hook-integration).
- **Why:** Adopt odb_library's documentation handling — frontmatter provenance, weekly change
  ledger, progressive-disclosure folding — so docs stay trustworthy and current.
- **Refs:** plan `below-is-the-camera-humble-sun.md`
